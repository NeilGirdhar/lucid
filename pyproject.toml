[project]
name = "lucid"
version = "0.1.0"
description = "Lucid language specification"
requires-python = ">=3.12"
dependencies = []

[tool.uv]
package = false

[dependency-groups]
dev = [
    "zensical>=0.0.60",
]
